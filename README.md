# PingFederate OAuth 2.0 Backend
This is a simple Node.js backend to authenticate users with PingFederate.

## Endpoints:
- `/login` → Redirects users to PingFederate login.
- `/callback` → Handles OAuth callback & token exchange.
